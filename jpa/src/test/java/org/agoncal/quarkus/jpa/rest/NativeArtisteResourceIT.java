package org.agoncal.quarkus.jpa.rest;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeArtisteResourceIT extends ArtistResourceTest {

    // Execute the same tests but in native mode.
}
